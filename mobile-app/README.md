# /mobile-app

Future: Android and iPad clients. React Native or Flutter, undecided.

Consumes `/core` through FFI (if the core is Rust) or directly (if TypeScript) — this choice is one input into the core-language decision in `docs/todo-app-spec.md` §6.

Not started — Windows ships first.
