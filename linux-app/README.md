# /linux-app

Future: a terminal client for Linux.

Open question (`docs/todo-app-spec.md` §6): either extend the existing Go/Bubbletea TUI project, or rebuild as a Rust TUI (ratatui) so it shares `/core` directly. The second option is preferred if `/core` lands in Rust.

Not started — Windows ships first.
